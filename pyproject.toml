[project]
name = "invoicer"
version = "0.1.0"
description = "PDF Invoice Generator with Email Integration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "reportlab>=4.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
    "msal>=1.24.0",
]
